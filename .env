# Database Configuration
DB_DRIVER=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=root
DB_PASSWORD=admin@123
DB_NAME=go_lang_project_setup
DB_LOG_MODE=true

# Server Configuration
SERVER_PORT=8080
SERVER_HOST=0.0.0.0
ENVIRONMENT=development

# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-change-in-production
JWT_EXPIRY_HOURS=24

# Security Configuration
CORS_ALLOWED_ORIGINS=*
CORS_ALLOWED_METHODS=GET,POST,PUT,DELETE,OPTIONS
CORS_ALLOWED_HEADERS=Content-Type,Authorization,X-Requested-With
RATE_LIMIT_REQUESTS=100
RATE_LIMIT_WINDOW_MINUTES=1

# Logging Configuration
LOG_LEVEL=info
LOG_FORMAT=json

# Redis Configuration (optional)
# REDIS_HOST=localhost
# REDIS_PORT=6379
# REDIS_PASSWORD=
# REDIS_DB=0

# LDAP Configuration
LDAP_HOST=172.105.49.219
LDAP_PORT=389
LDAP_BASE_DN=dc=dev,dc=local
LDAP_BIND_DN=cn=admin,dc=dev,dc=local
LDAP_BIND_PASSWORD=admin@123
LDAP_USER_FILTER=(uid=%s)
LDAP_GROUP_FILTER=(memberUid=%s)
LDAP_USE_SSL=false
LDAP_USE_TLS=false

# Mail Stack Configuration (Postfix/Dovecot)
# These are system-level configurations that may need to be set up separately
# The API will interact with these services via system commands 